
# detect signal before it reaches the soundcard. 
# if signal goes above a certain level, disconnect
# the signal going to the soundcard output, then 
# disconnect the incoming signal to this thing. 

# above a certain voltage?

local mainInput = Audio.MultiInput("mainInput", 2)



local detector = Audio.OnsetDetector()                                                 
detector.threshold = 0.9                                                               

mainInput => detector

// println(dectector)

Script.stayAlive()